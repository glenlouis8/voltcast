# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv

# Secrets — never commit
.env

# Claude instructions — keep local, never commit
CLAUDE.md

# Build plan — local notes, not tracked
BUILD_PLAN.md

# Model checkpoints — regenerable, live in DagsHub registry
checkpoints/

# Data files — too large for git, re-pulled / regenerated each run
data/raw/
data/features/
data/forecasts/
data/reference/

# MLflow experiment tracking
mlruns/
mlflow.db
mlartifacts/

# Evidently drift reports — regenerable
reports/

# Mac junk
.DS_Store
